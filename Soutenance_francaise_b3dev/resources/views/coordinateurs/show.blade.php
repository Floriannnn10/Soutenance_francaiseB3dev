@extends('layouts.app')@section('content')<div class="container mx-auto py-8">    @if(session('success'))        <div class="bg-green-100 border border-green-400 text-green-800 px-4 py-3 rounded mb-6">            {{ session('success') }}        </div>    @endif    <div class="max-w-2xl mx-auto bg-white rounded shadow p-8">        <div class="flex items-center mb-6">            <img src="{{ $coordinateur->photo ? asset('storage/'.$coordinateur->photo) : asset('images/default-avatar.png') }}" alt="Photo" class="w-20 h-20 rounded-full mr-6">            <div>                <h2 class="text-2xl font-bold mb-1">{{ $coordinateur->prenom }} {{ $coordinateur->nom }}</h2>                <div class="text-gray-600">Promotion : <span class="font-semibold">{{ $coordinateur->promotion ? $coordinateur->promotion->nom : '-' }}</span></div>            </div>        </div>        <div class="mb-4">            <h3 class="font-semibold mb-2">Classes associées à la promotion</h3>            @if($coordinateur->promotion && $coordinateur->promotion->classes->count())                <ul class="list-disc list-inside">                    @foreach($coordinateur->promotion->classes as $classe)                        <li class="mb-1">{{ $classe->nom }}</li>                    @endforeach                </ul>            @else                <span class="text-gray-400">Aucune classe pour cette promotion</span>            @endif        </div>        <div class="mt-6 flex gap-4">            <a href="{{ route('coordinateurs.edit', $coordinateur->id) }}" class="bg-yellow-400 hover:bg-yellow-500 text-white px-4 py-2 rounded">Modifier</a>            <a href="{{ route('coordinateurs.index') }}" class="bg-gray-300 hover:bg-gray-400 text-gray-800 px-4 py-2 rounded">Retour</a>        </div>    </div></div>@endsection
